FROM nginx:alpine

# Copy custom nginx config
COPY docker/nginx.conf /etc/nginx/conf.d/default.conf

# Copy all the frontend files to the nginx html directory
COPY . /usr/share/nginx/html

# Expose port 80
EXPOSE 80
